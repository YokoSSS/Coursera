Accessing DBMS Data with SAS-ACCESS


1.Which statement(s) is/are true about SAS/ACCESS technology? (Select all that apply)
1.SAS/ACCESSの技術について、正しいものはどれですか/正しいものはどれですか？(該当するものをすべて選択してください)

SAS/ACCESS software provides dynamic access to DBMS data through the SAS/ACCESS LIBNAME statement and the PROC SQL pass-through facility.
SAS/ACCESSソフトウェアは、SAS/ACCESS LIBNAMEステートメントとPROC SQLパススルー機能を通じて、DBMSデータへの動的アクセスを提供します。
Correct
SAS/ACCESS software provides dynamic access to DBMS data through the SAS/ACCESS LIBNAME statement and the PROC SQL pass-through facility. 
You do need to know your DBMS's SQL language to access and manipulate your DBMS data. It is faster and more direct to use the SAS/ACCESS 
LIBNAME statement to access your DBMS data. The LIBNAME statement provides more control over DBMS operations.
SAS/ACCESS ソフトウェアは、SAS/ACCESS LIBNAME ステートメントと PROC SQL パススルー機能を通じて DBMS データへの動的アクセスを提供します。
DBMSのデータにアクセスし、操作するためには、DBMSのSQL言語を知っている必要があります。SAS/ACCESS LIBNAME ステートメントを使用して、
DBMS データにアクセスする方が、より速く、より直接的です。LIBNAME ステートメントを使用すると、DBMS の操作をより詳細に制御することができます。

You need to know your DBMS's SQL language to access and manipulate your DBMS data.
DBMSのデータにアクセスし操作するためには、DBMSのSQL言語を知っている必要があります。
uncorrect
This should not be selected
SAS/ACCESS software provides dynamic access to DBMS data through the SAS/ACCESS LIBNAME statement and the PROC SQL pass-through facility. 
You do need to know your DBMS's SQL language to access and manipulate your DBMS data. It is faster and more direct to use the SAS/ACCESS 
LIBNAME statement to access your DBMS data. The LIBNAME statement provides more control over DBMS operations. Review Defining SAS/ACCESS Technology.
これは選択されるべきではありません
SAS/ACCESS ソフトウェアは、SAS/ACCESS LIBNAME ステートメントと PROC SQL パススルー機能を通じて DBMS データへの動的アクセスを提供します。
DBMS のデータにアクセスし、操作するには、DBMS の SQL 言語を知っている必要があります。SAS/ACCESS LIBNAME ステートメントを使用して、
DBMS データにアクセスする方が、より速く、より直接的です。LIBNAME文は、DBMSの操作をよりコントロールすることができます。SAS/ACCESSテクノロジーの定義を確認する。

It is faster and more direct to use the SAS/ACCESS LIBNAME statement to access your DBMS data.
SAS/ACCESS LIBNAMEステートメントを使用してDBMSデータにアクセスする方が、より速く、より直接的にアクセスすることができます。
Correct
SAS/ACCESS software provides dynamic access to DBMS data through the SAS/ACCESS LIBNAME statement and the PROC SQL pass-through facility. 
You do need to know your DBMS's SQL language to access and manipulate your DBMS data. It is faster and more direct to use the SAS/ACCESS 
LIBNAME statement to access your DBMS data. The LIBNAME statement provides more control over DBMS operations.
SAS/ACCESS ソフトウェアは、SAS/ACCESS LIBNAME ステートメントと PROC SQL パススルー機能を通じて DBMS データへの動的アクセスを提供します。
DBMSのデータにアクセスし、操作するためには、DBMSのSQL言語を知っている必要があります。SAS/ACCESS LIBNAME ステートメントを使用して、
DBMS データにアクセスする方が、より速く、より直接的です。LIBNAME ステートメントを使用すると、DBMS の操作をより詳細に制御することができます。


You do not need to know your DBMS's SQL implementation to access and manipulate your DBMS data.
DBMSのデータにアクセスし、操作するためにDBMSのSQLの実装を知る必要はありません。

不正解
You didn't select all the correct answers


2.What are some benefits of using the PROC SQL Pass-Through facility? (Select all that apply)
2.PROC SQL Pass-Through機能を使用する利点は何ですか？(該当するものを全て選択してください)

1点

The PROC SQL Pass-Through facility enables you to send DBMS-specific SQL statements directly to a DBMS for execution like you are coding inside of the database.
PROC SQL Pass-Through機能を使用すると、データベース内でコーディングするようにDBMS固有のSQL文を直接DBMSに送信して実行することができます。
Correct
The SQL Procedure pass-through facility is an extension of the SQL procedure that enables you to send DBMS-specific statements to a DBMS and 
to retrieve DBMS data. You specify DBMS SQL syntax instead of SAS SQL syntax when you use the pass-through facility. Review Overview of the 
SQL Pass-Through Facility .
SQLプロシージャのパススルー機能は、SQLプロシージャの拡張機能で、DBMS固有のステートメントをDBMSに送信し、DBMSのデータを取得することを可能にします。
パススルー機能を使用する場合、SAS SQL構文ではなく、DBMS SQL構文を指定します。SQL パススルー機能の概要を確認する .

The PROC SQL Pass-Through facility accepts all extensions to ANSI SQL that your DBMS provides.
PROC SQL Pass-Through機能は、DBMSが提供するANSI SQLの拡張をすべて受け入れることができます。
これも選択する

The PROC SQL Pass-Through facility lets the DBMS optimize queries when queries have summary functions.
PROC SQL Pass-Through機能は、クエリにサマリー関数がある場合、DBMSがクエリを最適化できるようにします。
Correct
The SQL Procedure pass-through facility is an extension of the SQL procedure that enables you to send DBMS-specific statements 
to a DBMS and to retrieve DBMS data. You specify DBMS SQL syntax instead of SAS SQL syntax when you use the pass-through facility. 
Review Overview of the SQL Pass-Through Facility .
SQLプロシージャのパススルー機能は、SQLプロシージャの拡張機能で、DBMS固有のステートメントをDBMSに送信し、DBMSのデータを取得することを可能にします。
パススルー機能を使用する場合、SAS SQL構文ではなく、DBMS SQL構文を指定します。SQL パススルー機能の概要を確認する .

不正解
You didn't select all the correct answers

3.Which query executes correctly in PROC FEDSQL?
3.PROC FEDSQLで正しく実行されるクエリはどれですか？
1点

proc fedsql;
select EmployeeID, Name, Salary, Department
    from orion.employee
    where = "Management";
quit;

proc fedsql;
select EmployeeID, Name, Salary, Department
    from orion.employee
    where = 'Management';
quit;
correct
正解
When using FEDSQL, you must use single quotes around a text string in the WHERE clause. Double quotes are a SAS enhancement and will not run in FEDSQL.
FEDSQL を使用する場合、WHERE 句のテキスト文字列の周囲には単一引用符を使用する必要があります。二重引用符はSASの機能拡張であり、FEDSQLでは実行されません。


4.You are working with multiple databases to pull data into SAS. The goal is to push as much processing into 
the database as possible and to keep the SQL queries vendor neutral. Which procedure would be better to use?
4.あなたは、SASにデータを取り込むために複数のデータベースで作業しています。目標は、
できるだけ多くの処理をデータベースに押し込み、SQLクエリをベンダーに依存しないようにすることです。どのプロシージャを使用するのが良いですか？


1点

PROC SQL
correct
不正解
PROC FEDSQL is a vendor neutral ANSI SQL that pushes as much of the processing as possible into the database. Review What Is PROC FEDSQL?
PROC FEDSQLはベンダーに依存しないANSI SQLであり、可能な限り多くの処理をデータベースにプッシュすることができます。レビュー PROC FEDSQLとは何か？

PROC FEDSQL





*******************************************************************************************************************************************************

1.Which statement(s) is/are true about SAS/ACCESS technology? (Select all that apply)

0 / 1点


You need to know your DBMS's SQL language to access and manipulate your DBMS data.

It is faster and more direct to use the SAS/ACCESS LIBNAME statement to access your DBMS data.
Correct
SAS/ACCESS software provides dynamic access to DBMS data through the SAS/ACCESS LIBNAME statement and the PROC SQL pass-through facility. 
You do need to know your DBMS's SQL language to access and manipulate your DBMS data. It is faster and more direct to use the SAS/ACCESS 
LIBNAME statement to access your DBMS data. The LIBNAME statement provides more control over DBMS operations.
SAS/ACCESS ソフトウェアは、SAS/ACCESS LIBNAME ステートメントと PROC SQL パススルー機能を通じて 
DBMS データへの動的アクセスを提供します。DBMSのデータにアクセスし、操作するためには、DBMSのSQL言語を知っている必要があります。
SAS/ACCESS LIBNAME ステートメントを使用して、DBMS データにアクセスする方が、より速く、より直接的です。LIBNAME ステートメントは、
DBMS の操作をよりコントロールすることができます。

SAS/ACCESS software provides dynamic access to DBMS data through the SAS/ACCESS LIBNAME statement and the PROC SQL pass-through facility.
Correct
SAS/ACCESS software provides dynamic access to DBMS data through the SAS/ACCESS LIBNAME statement and the PROC SQL pass-through facility. 
You do need to know your DBMS's SQL language to access and manipulate your DBMS data. It is faster and more direct to use the SAS/ACCESS 
LIBNAME statement to access your DBMS data. The LIBNAME statement provides more control over DBMS operations.
SAS/ACCESSソフトウェアは、SAS/ACCESS LIBNAMEステートメントとPROC SQLパススルー機能を通じて、DBMSデータへの動的なアクセスを提供します。
DBMSのデータにアクセスし、操作するためには、DBMSのSQL言語を知っている必要があります。SAS/ACCESS LIBNAME ステートメントを使用して、
DBMS データにアクセスする方が、より速く、より直接的です。LIBNAME ステートメントを使用すると、DBMS の操作をより詳細に制御することができます。

You do not need to know your DBMS's SQL implementation to access and manipulate your DBMS data.

不正解
You didn't select all the correct answers



2.You are attempting to pass Native Oracle SQL from SAS to the database for processing. 
To accomplish this task, the best option is to use the _________________ because it enables you to send database specific SQL 
statements directly to the database for execution.
2.あなたは、処理のためにSASからデータベースへネイティブのOracle SQLを渡そうとしています。
このタスクを達成するために、最良の選択肢は、______________を使用することです。
文を直接データベースに送信し、実行することができるからです。

1 / 1点

SQL Pass-Through Facility

INFILE statement

DBLOAD procedure

LIBNAME statement

正解
The SQL Procedure pass-through facility is an extension of the SQL procedure that enables you to send DBMS-specific statements to 
a DBMS and to retrieve DBMS data. You specify DBMS SQL syntax instead of SAS SQL syntax when you use the pass-through facility.
SQLプロシージャのパススルー機能は、SQLプロシージャの拡張機能で、DBMS固有のステートメントをDBMSに送信し、DBMSデータを取得することができます。
パススルー機能を使用する場合、SAS SQL 構文ではなく DBMS SQL 構文を指定します。


3.Which statement about FEDSQL is not true.

1 / 1点

FEDSQL supports all PROC SQL SAS enhancements.

FEDSQL supports many more data types than previous SAS SQL implementations.

FEDSQL conforms to the SQL 1999 ANSI standard.

FEDSQL is vendor neutral SAS SQL implementation


正解
FEDSQL conforms to the SQL 1999 ANSI standard. FEDSQL supports many more data types than previous SAS SQL implementations. 
FEDSQL is vendor neutral and handles federated queries, which access data from multiple data sources and returns a single 
result set. FEDSQL supports implicit SQL pass-through.
FEDSQL は、SQL 1999 ANSI 標準に準拠しています。FEDSQL は、以前の SAS SQL 実装よりも多くのデータ型をサポートしています。
FEDSQL はベンダーに依存せず、複数のデータ・ソースからデータにアクセスし、単一の結果セットを返すフェデレート・クエリーを処理します。
FEDSQL は、暗黙の SQL パススルーをサポートしています。


4.You are working with multiple databases to pull data into SAS. The goal is to push as much processing into the database as 
possible and to keep the SQL queries vendor neutral. Which procedure would be better to use?
4.あなたは、SASにデータを取り込むために、複数のデータベースを操作しています。目標は、できるだけ多くの処理をデータベースで行うことです。
SQLクエリをベンダーに依存しないようにすることです。どのプロシージャを使用するのが良いでしょうか？
1 / 1点

PROC SQL

PROC FEDSQL
correct
正解
PROC FEDSQL is a vendor neutral ANSI SQL that pushes as much of the processing as possible into the database.


*******************************************************************************************************************************************************

1.Which statement(s) is/are true about SAS/ACCESS technology? (Select all that apply)


It is faster and more direct to use the SAS/ACCESS LIBNAME statement to access your DBMS data.
Correct
SAS/ACCESS software provides dynamic access to DBMS data through the SAS/ACCESS LIBNAME statement and the PROC SQL pass-through facility. You do need to know your DBMS's SQL language to access and manipulate your DBMS data. It is faster and more direct to use the SAS/ACCESS LIBNAME statement to access your DBMS data. The LIBNAME statement provides more control over DBMS operations.

You need to know your DBMS's SQL language to access and manipulate your DBMS data.
uncorrect
This should not be selected
SAS/ACCESS software provides dynamic access to DBMS data through the SAS/ACCESS LIBNAME statement and the PROC SQL pass-through facility. You do need to know your DBMS's SQL language to access and manipulate your DBMS data. It is faster and more direct to use the SAS/ACCESS LIBNAME statement to access your DBMS data. The LIBNAME statement provides more control over DBMS operations. Review Defining SAS/ACCESS Technology.

SAS/ACCESS software provides dynamic access to DBMS data through the SAS/ACCESS LIBNAME statement and the PROC SQL pass-through facility.
Correct
SAS/ACCESS software provides dynamic access to DBMS data through the SAS/ACCESS LIBNAME statement and the PROC SQL pass-through facility. You do need to know your DBMS's SQL language to access and manipulate your DBMS data. It is faster and more direct to use the SAS/ACCESS LIBNAME statement to access your DBMS data. The LIBNAME statement provides more control over DBMS operations.

You do not need to know your DBMS's SQL implementation to access and manipulate your DBMS data.


不正解
You didn't select all the correct answers



2.What are some benefits of using the PROC SQL Pass-Through facility? (Select all that apply)


The PROC SQL Pass-Through facility accepts all extensions to ANSI SQL that your DBMS provides.
Correct
The SQL Procedure pass-through facility is an extension of the SQL procedure that enables you to send DBMS-specific statements to a DBMS and to retrieve DBMS data. You specify DBMS SQL syntax instead of SAS SQL syntax when you use the pass-through facility. Review Overview of the SQL Pass-Through Facility .

The PROC SQL Pass-Through facility lets the DBMS optimize queries when queries have summary functions.
Correct
The SQL Procedure pass-through facility is an extension of the SQL procedure that enables you to send DBMS-specific statements to a DBMS and to retrieve DBMS data. You specify DBMS SQL syntax instead of SAS SQL syntax when you use the pass-through facility. Review Overview of the SQL Pass-Through Facility .

The PROC SQL Pass-Through facility enables you to send DBMS-specific SQL statements directly to a DBMS for execution like you are coding inside of the database.
Correct
The SQL Procedure pass-through facility is an extension of the SQL procedure that enables you to send DBMS-specific statements to a DBMS and to retrieve DBMS data. You specify DBMS SQL syntax instead of SAS SQL syntax when you use the pass-through facility. Review Overview of the SQL Pass-Through Facility .


正解
Great, you got all the right answers.



3.Which query executes correctly in PROC FEDSQL?



proc fedsql;
select EmployeeID, Name, Salary, Department
    from orion.employee
    where = 'Management';
quit;
correct


proc fedsql;
select EmployeeID, Name, Salary, Department
    from orion.employee
    where = "Management";
quit;


正解
When using FEDSQL, you must use single quotes around a text string in the WHERE clause. Double quotes are a SAS enhancement and will not run in FEDSQL.


4.Which of the following is true regarding PROC FEDSQL?


It is vendor-neutral ANSI SQL.

It includes very few non-ANSI SAS enhancements.

It processes 17 ANSI data types.

All of the above are true.
correct

正解
They are all true. PROC FEDSQL is vendor neutral ANSI SQL, is ANSI Standard 3 compliant, processes 17 ANSI data types, is fully multi-threaded on the SAS platform, includes very few non-ANSI SAS enhancements, and executes in CAS.


************************************************************************************************************************************************
Accessing DBMS Data with SAS/ACCESS

1.Which statement(s) is/are true about SAS/ACCESS technology? (Select all that apply)

1 / 1点



You do not need to know your DBMS's SQL implementation to access and manipulate your DBMS data.
Correct
SAS/ACCESS software provides dynamic access to DBMS data through the SAS/ACCESS LIBNAME statement 
and the PROC SQL pass-through facility. You do need to know your DBMS's SQL language to access and 
manipulate your DBMS data. It is faster and more direct to use the SAS/ACCESS LIBNAME statement to
access your DBMS data. The LIBNAME statement provides more control over DBMS operations.


It is faster and more direct to use the SAS/ACCESS LIBNAME statement to access your DBMS data.
Correct
SAS/ACCESS software provides dynamic access to DBMS data through the SAS/ACCESS LIBNAME statement 
and the PROC SQL pass-through facility. You do need to know your DBMS's SQL language to access and 
manipulate your DBMS data. It is faster and more direct to use the SAS/ACCESS LIBNAME statement to 
access your DBMS data. The LIBNAME statement provides more control over DBMS operations.


You need to know your DBMS's SQL language to access and manipulate your DBMS data.

SAS/ACCESS software provides dynamic access to DBMS data through the SAS/ACCESS LIBNAME 
statement and the PROC SQL pass-through facility.
Correct

SAS/ACCESS software provides dynamic access to DBMS data through the SAS/ACCESS LIBNAME statement 
and the PROC SQL pass-through facility. You do need to know your DBMS's SQL language to access and 
manipulate your DBMS data. It is faster and more direct to use the SAS/ACCESS LIBNAME statement to 
access your DBMS data. The LIBNAME statement provides more control over DBMS operations.


正解
Great, you got all the right answers.



2.You are attempting to pass Native Oracle SQL from SAS to the database for processing. 
To accomplish this task, the best option is to use the _________________ because it enables you to 
send database specific SQL statements directly to the database for execution.

1 / 1点

SQL Pass-Through Facility
correct
正解
The SQL Procedure pass-through facility is an extension of the SQL procedure that enables you to send 
DBMS-specific statements to a DBMS and to retrieve DBMS data. You specify DBMS SQL syntax instead of 
SAS SQL syntax when you use the pass-through facility.


INFILE statement

LIBNAME statement

DBLOAD procedure


3.PROC FEDSQL provides the same SAS enhancements as PROC SQL.

1 / 1点

True

False
correct
正解
PROC FEDSQL is a SAS vendor neutral SQL implementation and does not contain all PROC SQL SAS enhancements.



4.You are working with multiple databases to pull data into SAS. The goal is to push as much processing 
into the database as possible and to keep the SQL queries vendor neutral. Which procedure would be better to use?


1 / 1点

PROC SQL

PROC FEDSQL
correct
正解
PROC FEDSQL is a vendor neutral ANSI SQL that pushes as much of the processing as possible into the database.






