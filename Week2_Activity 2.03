Activity 2.03

1.If necessary, start SAS Studio before you begin. If you restarted your SAS session, 
submit your libname.sas program to access the practice data.
必要であれば、事前に SAS Studio を起動してください。
SAS セッションを再起動した場合は、練習データにアクセスするために libname.sas プログラムを提出します。

Open p302a03.sas from the activities folder and perform the following tasks:
activities フォルダから p302a03.sas を開き、以下のタスクを実行します。
Run the program and verify that Open3MnthAvg is equal to missing for the first two rows.
プログラムを実行し、最初の 2 行で Open3MnthAvg が missing と等しいことを確認します。
What is its value for the third row? Note: Type your answer as it appears in the results.
3行目の値は何ですか？注：結果に表示されるように、あなたの答えを入力してください。

1点
こちらに回答を入力
102.90


2.Modify the conditional statement to include the calls to LAG1 and LAG2, 
and delete the two assignment statements containing LAG functions.
条件文にLAG1、LAG2の呼び出しを含めるように修正する。そして、LAG関数を含む2つの代入文を削除する。

Run the program and view the results. 
プログラムを実行し、結果を表示します。

Is Open3MnthAvg equal to 102.90 for 02MAR2010 as in the previous activity?
Open3MnthAvgは、前のアクティビティと同様に02MAR2010の102.90に等しいですか？

1点

Yes


No
correct
104.19

正解
No, it is 104.19, the same value as Open for that month.  

/*
data work.stockmovingavg;
    set pg3.stocks_ABC(drop=Close);
    Open1MnthBack=lag1(Open);
    Open2MnthBack=lag2(Open);
   if _N_ ge 3 then
       Open3MnthAvg=mean(Open,lag1(Open),lag2(Open));
    format Open3MnthAvg 8.2;
run;

title 'Three Month Moving Average on Opening Stock Price'; 
proc print data=work.stockmovingavg noobs;
run;
title;
*/

