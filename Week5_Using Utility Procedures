
1.Which of the following statements is true regarding PICTURE statement directives?
PICTURE文のディレクティブに関する次の記述のうち、正しいものはどれか。

Directives are special characters used to format date, time, or date time values.
ディレクティブは、日付、時間、日付時刻の値をフォーマットするために使用される特殊文字である。
uncorrect
不正解
Both statements regarding PICTURE statement directives are true. Review Custom Date and Datetime Formats.
PICTUREステートメントディレクティブに関する記述は、どちらも正しいです。カスタム日付と日付時間の形式を見直す。

Directives can only be used when the DATATYPE= option enclosed in parentheses has been specified in the PICTURE statement.
指令は、PICTURE文の中で括弧で囲まれたDATATYPE=オプションが指定されているときだけ使用できる。

Neither statement is true.
どちらの記述も正しくない。

The first two statements regarding the PICTURE statement directives are true.
PICTURE文のディレクティブに関する最初の2つの記述は、真である。



2.Which PICTURE statement generates the following results when the custom format is used with the Number column? 
2.カスタムフォーマットをNumber列で使用する場合、どのPICTURE文が次の結果を生成するか。

Number

76.54

0.32

1.82


picture FmtC low-high='009.99';

picture FmtB low-high='000.00';

all of the above
uncorrect
不正解
A nonzero digit selector must precede the decimal point. Using a zero digit selector before the decimal point 
suppresses the decimal point. Also, using a non-zero digit selector in the tens and hundreds position requires 
that a number be printed in that place. Review Custom Numeric Formats.
小数点以下はゼロ以外の数字で指定する必要があります。小数点の前に0桁のセレクタを使用すると、小数点が抑制されます。
また、十の位と百の位にゼロ以外の数字セレクタを使用すると、その位置に数字を印刷する必要があります。
カスタム数値フォーマットを確認する。


picture FmtA low-high='999.99';

3.Which PICTURE statement displays a ten-digit number with a hyphen (dash) between the third and 
fourth digit and between the sixth and seventh digit, such as 555-555-5555?
555-555-5555のように、3桁目と4桁目、6桁目と7桁目の間にハイフン（ダッシュ）を入れた10桁の数字を表示するPICTURE文はどれでしょうか。

1点
picture phonefmt
   low-high='999' (prefix='-');

picture phonefmt
   low-high='999999999' (prefix='-');

picture phonefmt
   low-high = '999-999-9999';

picture phonefmt
   low-high (prefix='999-999–9999');
uncorrect
不正解
The pattern in quotation marks must include digit selectors for all numbers along with the displayed symbols. 
No prefix is necessary for this format. Review Custom Large Number Format.
引用符で囲まれたパターンは、表示される記号とともに、すべての数値の桁選択記号を含まなければならない。
このフォーマットでは、接頭辞は必要ありません。カスタムラージナンバーフォーマットを確認する。


4.Which of the following is a format option that is specified after the format name?
形式名の後に指定する形式オプションは次のうちどれですか? 

DEFAULT=

DATATYPE=

MULTIPLIER=
uncorrect
不正解
MULTIPLIER= is not placed after the format name. It is a template option that is placed after the 
template value. MULTIPLIER= specifies a number by which to multiply the value. Review Custom Numeric Formats.
MULTIPLIER=は、フォーマット名の後には置かれません。これはテンプレートオプションであり、テンプレート値の後に置かれる。
MULTIPLIER=は、値を乗算する数値を指定します。カスタム数値書式を見直す

PREFIX=


5.Which PICTURE statement generates a date format that produces the following results when used with the Date column? 
どのPICTURE文が、Date列と一緒に使用すると、次のような結果をもたらす日付フォーマットを生成するか。

Date

Monday - March 9, 2020

Wednesday - November 3, 2021


proc format;
    picture longDate (default=30) low-high =
       '%A - %B %d, %Y';

proc format;
    picture longDate
       (default=30) low-high = '%A - %B %d, %Y'
       (datatype=date);
correct
正解
Without the DEFAULT= option, the length for the format is 8 characters, so a longer default is necessary. 
The DEFAULT= option goes after the format name. The DATATYPE= value must match the data, which is DATE in 
this example. %A displays the full weekday name, %B displays the full month name, %d displays the one or 
two digit day of the month, and %Y displays a four-digit year.
DEFAULT=オプションがない場合、フォーマットの長さは8文字ですので、より長いデフォルトが必要です。
DEFAULT=オプションは書式名の後に付けます。DATATYPE=値はデータと一致しなければならず、この例ではDATEになっています。
Aは完全な曜日名、%Bは完全な月名、%dは1桁または2桁の月日、%Yは4桁の年号を表示します。

proc format;
    picture longDate
       low-high = '%A - %B %m, %Y' (datatype=date);

proc format;
    picture longDate (default=30) low-high
       (datatype=date);



6.Which option is necessary in the following PROC FCMP step to complete the ToCelsius function? 
ToCelsius関数を完成させるために、次のPROC FCMPステップで必要なオプションはどれですか？


proc fcmp __________ lib3.func.weather;
    function ToCelsius(Fah);
        Cel=round((Fah-32)*5/9,.01);
        return(Cel);
    endsub;
run;

CMPLIB=

LIBFUNC=
uncorrect
不正解
The OUTLIB= option in the PROC FCMP statement specifies the three-level name of the package where 
the compiled function will be stored. Review Creating and Using a Custom Function.
PROC FCMP ステートメントの OUTLIB= オプションは、コンパイルされた関数が格納されるパッケージの 
3 階層名を指定します。カスタム関数の作成と使用について復習します。

LIB=

OUTLIB=



7.Complete the FUNCTION statement to create a function named CnvTemp that consists of a numeric argument followed by a character argument. 
FUNCTIONステートメントを完成させて、CnvTempという名前の、数値引数と文字引数の組み合わせからなる関数を作成します。

function CnvTemp _______________;
    if uppercase(Unit)= 'F'
        then NewTemp=round((Temp-32)*5/9,.01);
    else if upcase(Unit)= 'C'
        then NewTemp=round(Temp*9/5+32,.01);
    return(NewTemp);
endsub;

1点

('Temp', 'Unit' )

(Temp $, Unit $)

(Temp $, 'Unit')
uncorrect
不正解
Temp is a numeric argument, while Unit is a character argument. Character arguments must be followed 
by a dollar sign and separated with a comma. No quotation marks are needed. Review Passing and Returning Values.
Temp は数値引数、Unit は文字引数です。文字引数は、ドル記号の後にカンマで区切る必要があります。
引用符は必要ありません。値の受け渡しと戻り値について復習しましょう。

(Temp, Unit $)


8.Which of the following statements is true?
8.次の記述のうち、正しいものはどれか。

Neither functions nor CALL routines accept arguments.
関数と CALL ルーチンはどちらも引数を受け取りません。

Only functions return a value.
関数のみが値を返します。

Only CALL routines perform operations.
CALL ルーチンのみが操作を実行します。

Both functions and CALL routines accept arguments and return values.
関数とCALLルーチンの両方が、引数と戻り値を受け取ります。
不正解
Both functions and CALL routines can accept arguments and perform operations. 
However, only functions can return a value. Review Creating and Using a Custom Function.
関数とCALLルーチンはどちらも引数を受け取り、操作を実行することができます。
しかし、値を返すことができるのは関数だけです。カスタム関数の作成と使用について復習します。


9.Which statement specifies that SAS search the mylib.funcs table for a package that contains a custom function?
9.SASがカスタム関数を含むパッケージのmylib.funcテーブルを検索することを指定するステートメントはどれですか？

options cmplib=mylib.funcs;

proc fcmp lib=mylib.funcs;

proc fcmp cmplib=mylib.funcs;
uncorrect
不正解
The CMPLIB= option specifies the table (or tables) that SAS searches for a package that contains 
the desired function. Review Creating and Using a Custom Function.
CMPLIB= オプションは、SAS が目的の関数を含むパッケージを検索するテーブル（複数可）を指定します。
カスタム関数の作成と使用について復習します。

options functions=mylib.funcs;



10.Which statement is true concerning the following PROC FCMP statement? 
10.PROC FCMPの次の文について、正しいものはどれか。

proc fcmp outlib=pg3.funcs.misc; 


The custom function will be stored in the SAS table pg3.misc.
カスタム関数は、SAS テーブル pg3.misc に格納されます。

The custom function will be stored in the misc package within the SAS table pg3.funcs.
カスタム関数は、SAS テーブル pg3.funcs 内の misc パッケージに格納されます。
correct
正解
Custom functions are stored in a package within a SAS table. The OUTLIB= option consists of a 
three-level name. The first two levels refer to the SAS table ( libref.tablename ), and the third level refers to the package.
カスタム関数は、SASテーブル内のパッケージに格納されます。OUTLIB=オプションは、3レベルの名前から構成されます。
最初の2レベルはSASテーブル（libref.tablename）を参照し、3レベル目はパッケージを参照します。


The custom function will be stored in the pg3 package within the SAS table funcs.misc.
カスタム関数は、pg3パッケージのSASテーブルfunc.miscに格納されます。

The custom function will be stored in the misc folder within the SAS package pg3.funcs.
カスタム関数は、SAS パッケージ pg3.funcs 内の misc フォルダに格納されます。



************************************************************************************************************************************



1.Which statement is true regarding the PICTURE statement?
PICTUREステートメントについて、正しい記述はどれか。

It is best to set the DEFAULT option within the PICTURE statement equal to the smallest possible formatted value.
PICTURE文のDEFAULTオプションは、可能な限り小さな書式値と等しく設定するのが最良である。
不正解
The DEFAULT option should be set to accommodate the largest possible formatted value. 
Review Creating Custom Formats and Custom Date and Datetime Formats.
DEFAULTオプションは、可能な限り大きなフォーマット値に対応するように設定する必要があります。
カスタム書式の作成とカスタム日付・日時書式を確認する。

The PICTURE statement creates a format that specifies a template for displaying character values.
PICTURE文は、文字値を表示するための雛形を指定する書式を作成する。

The PICTURE statement is valid only in the FORMAT procedure.
PICTURE文は、FORMAT手続き内だけで有効である。

Numeric values may be formatted using a PICTURE statement within the DATA step.
数値は、DATAステップの中でPICTURE文を用いて書式設定することができる。



2.Which PICTURE statement generates the following results when the custom format is used with the Number column? 
2.カスタムフォーマットをNumber列で使用する場合、どのPICTURE文が次の結果を生成するか。

Number

76.54

0.32

1.82


picture FmtC low-high='009.99';

picture FmtA low-high='999.99';
uncorrect
不正解
Using a non-zero digit selector in the tens and hundreds position requires that a 
number be printed in that place. Review Custom Numeric Formats.
十の位と百の位に0でない数字セレクタを使用すると、その場所に数字を印刷する必要があります。
カスタム数値フォーマットを確認する。

picture FmtB low-high='000.00';

all of the above




3.Which PICTURE statement displays a nine-digit account number with five leading asterisks followed by 
the last four digits, such as *****6789? 
PICTURE文の中で、*****6789のように、先頭のアスタリスク5桁と下4桁で9桁の口座番号を表示するものはどれでしょう？



picture maskfmt
    low-high='999999999' (prefix='*****');

picture maskfmt
    low-high='9999' (prefix='*****');

picture maskfmt
    (prefix='*****') low-high='999999999';
uncorrect
不正解
Only four digit selectors should be used in quotation marks display the last 4 numbers. 
The PREFIX= option specifies a character string to place in front of the formatted value. 
The PREFIX= option must be after the template. Review Custom Large Number Format.
4桁のセレクタのみ引用符で囲んで使用し、最後の4桁の数字を表示させる必要があります。
PREFIX=オプションは、フォーマットされた値の前に置く文字列を指定します。PREFIX=オプションは、
テンプレートの後に付ける必要があります。Custom Large Number Formatをレビューします。


picture maskfmt
    (prefix='*****') low-high='9999';





4.Which of the following is a format option that specifies a number by which to multiply the value? Select all that apply.
値を乗じる数値を指定する書式オプションは、次のうちどれでしょうか。該当するものをすべて選んでください。



MULTIPLIER=
Correct
MULTIPLIER= is a format option that specifies a number by which to multiply the value. MULT= is an alias for MULTIPLIER=.
MULTIPLIER=は、値を乗算する数値を指定するフォーマットオプションです。MULT=はMULTIPLIER=の別名である。

MULT=
Correct
MULT= is an alias for MULTIPLIER=.
MULT=は、MULTIPLIER=の別名です。

MULTIPLY=

*=

正解
Great, you got all the right answers.


    

5.Which PICTURE statement generates a date format that produces the following results when used with the Date column? 
どのPICTURE文が、Date列と一緒に使用すると次のような結果を生む日付書式を生成するか。

Date

Monday - March 9, 2020

Wednesday - November 3, 2021




proc format;
    picture longDate
       (default=30) low-high = '%A - %B %d, %Y'
       (datatype=date);

proc format;
    picture longDate
       low-high = '%A - %B %m, %Y' (datatype=date);

proc format;
    picture longDate (default=30) low-high =
       '%A - %B %d, %Y';
不正解
The DATATYPE= option must be specified to use custom date directives. 
The DATATYPE= value must match the data, which is DATE in this example. Review Custom Date and Datetime Formats.
カスタム日付ディレクティブを使用するには、DATATYPE= オプションを指定する必要があります。
DATATYPE= 値は、データ (この例では DATE) と一致しなければなりません。カスタム日付と日付時間の形式を確認する。

proc format;
    picture longDate (default=30) low-high
       (datatype=date);



6.Which statement is necessary in the following PROC FCMP step to begin the ToCelsius function definition? 
次の PROC FCMP ステップで、ToCelsius 関数定義を開始するために必要なステートメントはどれですか。


proc fcmp outlib=lib3.func.weather;
    __________ ToCelsius(Fah);
        Cel=round((Fah-32)*5/9,.01);
        return(Cel);
    endsub;
run;


DEF

FUNCTION
correct
正解
The FUNCTION statement begins the function definition. It names the function and defines the arguments and a return value.
FUNCTION文は、関数定義を開始します。これは、関数に名前を付け、引数と戻り値を定義します。

STARTFUNC

FUNCDEF



7.Complete the FUNCTION statement to create a function named sumfunc that consists of two numeric arguments. 
7.FUNCTIONステートメントを完成させ、2つの数値引数からなるsumfuncという名前の関数を作成します。

function sumfunc _______________;
    sum = addend1+addend2;
    return(sum);
endsub;

1点

(addend1 $ addend2 $)

(addend1 addend2)

(addend1, addend2)
正解
Arguments must be separated with a comma. Numeric arguments are not followed by a dollar sign.
引数はカンマで区切らなければなりません。数値引数の後にはドル記号を付けない。


(addend1 $, addend2 $)




8.The RETURN statement can have the custom function return multiple values.
8.RETURN文は、カスタム関数に複数の値を返させることができます。

True
不正解
The RETURN statement can return only a single value. Review Creating and Using a Custom Function.
RETURN文は、単一の値のみを返すことができます。カスタム関数の作成と使用について復習してください。


False



9.Which statement specifies that SAS search the mylib.funcs table for a package that contains a custom function?
9.SASがカスタム関数を含むパッケージのmylib.funcテーブルを検索することを指定するステートメントはどれですか？

options cmplib=mylib.funcs;
correct
正解
The CMPLIB= option specifies the table (or tables) that SAS searches for a package that contains the desired function.
CMPLIB=オプションは、SASが目的の関数を含むパッケージを検索するテーブル（複数可）を指定します。

options functions=mylib.funcs;

proc fcmp cmplib=mylib.funcs;

proc fcmp lib=mylib.funcs;



10.Which statement is true concerning the following PROC FCMP statement? 

proc fcmp outlib=pg3.funcs.misc; 


The custom function will be stored in the pg3.funcs table.
カスタム関数は pg3.funcs テーブルに格納されます。

The custom function will be stored in the pg3.funcs package.
カスタム関数は、pg3.func パッケージに格納されます。
uncorrect
不正解
Custom functions are stored in a package within a SAS table. The OUTLIB= option consists of a three-level name. 
The first two levels refer to the SAS table ( libref.tablename ), and the third level refers to the package. 
Review Creating and Using a Custom Function.
カスタム関数は、SASテーブル内のパッケージに格納されます。OUTLIB=オプションは、3レベルの名前から構成されます。
最初の 2 レベルは SAS テーブル ( libref.tablename ) を参照し、3 レベルはパッケージを参照します。
カスタム関数の作成と使用について復習します。

The custom function will be stored in the misc library.
カスタム関数は、miscライブラリに格納されます。

The custom function will be stored in the SAS table pg3.
カスタム関数は、SAS テーブル pg3 に格納されます。


*******************************************************************************************************************************

1.Which statement is true regarding the PICTURE statement?

1点

It is best to set the DEFAULT option within the PICTURE statement equal to the smallest possible formatted value.

Numeric values may be formatted using a PICTURE statement within the DATA step.

The PICTURE statement creates a format that specifies a template for displaying character values.
不正解
The PICTURE statement creates a format that specifies a template for displaying numeric values.
Review Creating Custom Formats and Custom Date and Datetime Formats.
PICTURE文は、数値を表示するためのテンプレートを指定する書式を作成します。カスタム書式の作成とカスタム日付・日時書式を確認する。

The PICTURE statement is valid only in the FORMAT procedure.



2.Which PICTURE statement generates the following results when the custom format is used with the Number column? 

Number

76.54

0.32

1.82

1点


all of the above

picture FmtC low-high='009.99';
正解
A nonzero digit selector must precede the decimal point. Using a zero digit selector before the decimal 
point suppresses the decimal point.
小数点以下はゼロ以外の数字で指定する必要があります。小数点の前に0桁のセレクタを使用すると、小数点が抑制されます。

picture FmtB low-high='000.00';

picture FmtA low-high='999.99';



3.Which PICTURE statement displays a nine-digit account number with five leading asterisks 
followed by the last four digits, such as *****6789? 
3.どのPICTURE文が、*****6789のように、先頭の5つのアスタリスクと最後の4桁で9桁の口座番号を表示するか。
1点



picture maskfmt
    (prefix='*****') low-high='9999';

picture maskfmt
    (prefix='*****') low-high='999999999';

picture maskfmt
    low-high='999999999' (prefix='*****');

picture maskfmt
    low-high='9999' (prefix='*****');
correct
正解
The four-digit digit selector truncates the leading digits, and the PREFIX= option specifies 
a character string to place in front of the formatted value. The PREFIX= option must be after
the template. Review Custom Large Number Format.
4桁の数字セレクタは先頭の数字を切り捨て、PREFIX=オプションはフォーマットされた値の前に置く文字列を指定します。
PREFIX=オプションはテンプレートの後に付ける必要があります。Custom Large Number Formatを見直す。    



4.Which of the following format options enables the use of directives to customize date, time, or datetime values?
4.次の書式オプションのうち、ディレクティブを使用して日付、時刻、またはdatetimeの値をカスタマイズできるものはどれですか？
1点

DATATYPE=

DATETYPE=
uncorrect
不正解
DATETYPE= is not a valid option. DATATYPE= enables the use of directives to format date, 
time, or datetime values. Review Custom Date and Datetime Formats.
DATETYPE= は有効なオプションではありません。DATATYPE= は、日付、時刻、または datetime 値を
フォーマットするためのディレクティブを使用できるようにします。カスタム日付・時刻形式を確認する。

None of the choices are correct.

DEFAULT=


5.Which PICTURE statement generates a date format that produces the following results when used with the Date column? 
5.どのPICTURE文が、Date列と一緒に使用すると、次の結果を生成する日付書式を生成しますか。

Date

9/8/2019

10/24/2019

1点


picture NoZero
    low-high='%m/%d/%Y' (datatype=date);

picture NoZero
    (default=10 )low-high='%m/%d/%Y'
    (datatype=date);
correct
正解
Without the DEFAULT= option, the length for the format is 8 characters, but a 10-character 
length is needed. The DEFAULT= option goes after the format name. The DATATYPE= value must
match the data, which is DATE in this example. %m displays a one- or two-digit month, %d displays 
a one- or two-digit day, and %Y displays a four-digit year.
DEFAULT= オプションがない場合、フォーマットの長さは 8 文字ですが、10 文字の長さが必要です。
DEFAULT=オプションは書式名の後に付けます。DATATYPE=値はデータと一致しなければならず、
この例ではDATEである。mは1桁または2桁の月、%dは1桁または2桁の日、%Yは4桁の年を表示します。


picture NoZero
    (default=10) low-high='%m/%d/%Y'
    (datatype=datetime);

picture NoZero
    low-high='%m/%d/%Y' (datatype=date default=10);
    

6.Which statement is needed in the following PROC FCMP step to end the syntax for the ToCelsius function? 
6.ToCelsius 関数の構文を終了するために、次の PROC FCMP ステップでどのステートメントが必要ですか。

proc fcmp outlib=pg3.funcs.tempconvert;  
    function ToCelsius(Fah);
        Cel=round((Fah-32)*5/9,.01);
        return(Cel);
        _____________________
run; 

1点

END;

ENDFUNC;

ENDSUB;
correct
正解
The ENDSUB statement ends the syntax for a custom function.


ENDFUNCTION;



7.Complete the FUNCTION statement to create a function named sumfunc that consists of two numeric arguments. 

function sumfunc _______________;
    sum = addend1+addend2;
    return(sum);
endsub;

1点

(addend1 addend2)

(addend1, addend2)
正解
Arguments must be separated with a comma. Numeric arguments are not followed by a dollar sign.
引数はカンマで区切らなければならない。数値引数の後にはドル記号を付けない。


(addend1 $ addend2 $)

(addend1 $, addend2 $)



8.Which of the following statements is true?
8.次の記述のうち、正しいものはどれですか？
1点

Both functions and CALL routines accept arguments and return values.

Only CALL routines perform operations.

Only functions return a value.
正解
Both functions and CALL routines can accept arguments and perform operations. However, only functions can return a value.
関数とCALLルーチンは、どちらも引数を受け取り、演算を行うことができます。しかし、関数のみが値を返すことができます。

Neither functions nor CALL routines accept arguments.




9.What is the function of the CMPLIB= option?
9.CMPLIB=オプションの機能は何ですか？
1点

The CMPLIB= option specifies the column (or columns) that SAS searches for a package that contains the desired function.
CMPLIB= オプションは、目的の関数を含むパッケージを SAS が検索する列（1 つまたは複数の列）を指定します。

The CMPLIB= option specifies the table (or tables) that SAS searches for a package that contains the desired function.
CMPLIB= オプションは、SAS が希望する関数を含むパッケージを検索するテーブルを指定します。
The CMPLIB= option specifies the table (or tables) that SAS searches for a package that contains the desired function.
CMPLIB= オプションは、目的の関数が含まれるパッケージを SAS が検索するテーブル（1 つまたは複数）を指定します。
正解

The CMPLIB= option specifies the two-level name of the package where the compiled function will be stored.
CMPLIB= オプションは、コンパイルされた関数が格納されるパッケージの 2 階層名を指定します。

The CMPLIB= option specifies the three-level name of the package where the compiled function will be stored.
CMPLIB= オプションは、コンパイルされた関数が格納されるパッケージの 3 階層名を指定します。



10.Which statement is true concerning the following PROC FCMP statement? 
10.次のPROC FCMP文に関する記述のうち、正しいものはどれか。

proc fcmp outlib=pg3.funcs.misc; 

1点

The custom function will be stored in the pg3.funcs table.
カスタム関数は、pg3.funcテーブルに格納されます。

The custom function will be stored in the SAS table pg3.
カスタム関数は、SAS テーブル pg3 に格納されます。

The custom function will be stored in the pg3.funcs package.
カスタム関数は、pg3.func パッケージに格納されます。

The custom function will be stored in the misc library.
カスタム関数は、misc ライブラリに格納されます。
不正解
Custom functions are stored in a package within a SAS table. The OUTLIB= option consists 
of a three-level name. The first two levels refer to the SAS table ( libref.tablename ),
and the third level refers to the package. Review Creating and Using a Custom Function.
カスタム関数は、SASテーブル内のパッケージに格納されます。OUTLIB=オプションは、
3レベルの名前から構成されます。最初の 2 レベルは SAS テーブル ( libref.tablename ) を参照し、
3 レベルはパッケージを参照します。カスタム関数の作成と使用について復習します。

********************************************************************************************************************************


1.Which statement about the PICTURE statement is false?
PICTURE文について、誤っている文はどれか。
0 / 1点


The PICTURE statement creates a format that specifies a template for displaying numeric values.
PICTURE文は、数値を表示するためのテンプレートを指定する書式を作成します。
uncorrect

The PICTURE statement can create custom date, time, and datetime formats.
PICTURE文は、カスタムの日付、時刻、日時の書式を作成することができます。

The PICTURE statement can be used only in the PROC FORMAT step.
PICTURE文は、PROC FORMATステップでのみ使用することができます。

The PICTURE statement specifies character strings to display for character values.
PICTURE文は、文字値に対して表示する文字列を指定します。


不正解
This statement is true. The PICTURE statement creates a format that specifies a template for displaying numeric values, 
including dates, times, and datetimes. Review  and .
このステートメントは真です。PICTUREステートメントは、日付、時間、およびdatetimesを含む数値を表示するためのテンプレートを指定する形式を作成します。復習と.


2.Which PICTURE statement generates the following results when the custom format is used with the Number column? 
カスタムフォーマットをNumber列で使用する場合、どのPICTURE文が次の結果を生成しますか？

Number

76.54

0.32

1.82


all of the above

picture FmtA low-high='999.99';

picture FmtB low-high='000.00';

picture FmtC low-high='009.99';
correct

1 / 1点

正解
A nonzero digit selector must precede the decimal point. Using a zero digit selector before the decimal point suppresses the decimal point.
小数点以下はゼロ以外の数字セレクタを使用しなければなりません。小数点の前に0桁のセレクタを使用すると、小数点が抑制されます。

3.Which PICTURE statement displays a ten-digit number with a hyphen (dash) between the third and fourth digit and between the sixth and 
seventh digit, such as 555-555-5555?
3.555-555-5555のように、3桁目と4桁目、6桁目と7桁目の間にハイフン（ダッシュ）を入れて10桁の数字を表示するPICTURE文はどれ？
0 / 1点


picture phonefmt
   low-high = '999-999-9999';

picture phonefmt
   low-high='999' (prefix='-');

picture phonefmt
   low-high='999999999' (prefix='-');
uncorrect

picture phonefmt
   low-high (prefix='999-999–9999');

不正解
The pattern in quotation marks must include digit selectors for all numbers along with the displayed symbols. 
No prefix is necessary for this format. Review .
引用符で囲まれたパターンは、表示される記号とともに、すべての数字の桁選択記号を含んでいなければならない。この形式では、接頭辞は必要ありません。復習.


4.Which of the following is a format option that is specified after the format name?
4.書式名の後に指定する書式オプションは次のうちどれでしょうか。
0 / 1点


PREFIX=

DATATYPE=
uncorrect

DEFAULT=

MULTIPLIER=

不正解
DATATYPE= is not placed after the format name. 
It is a template option that is placed after the template value. 
DATATYPE= enables the use of directives to format date, time, or datetime values. Review .
DATATYPE=は、フォーマット名の後には置かれません。
これは、テンプレート値の後に置かれるテンプレート・オプションである。
DATATYPE=は、日付、時刻、または日時の値をフォーマットするためのディレクティブを使用できるようにします。復習 。


5.Which of the following formatted values does not require the DEFAULT= option when defining the format?
5.次のフォーマット値のうち、フォーマットを定義する際にDEFAULT=オプションを必要としないものはどれですか？
1 / 1点


April 30, 2011

Fri Jun 23 06

10/27/10
correct

1992-06-08

正解
Without the DEFAULT= option, the length for the format is 8 characters, which will accommodate this date.
DEFAULT=オプションがない場合、書式の長さは8文字で、この日付に対応します。


6.Which statement is necessary in the following PROC FCMP step to begin the ToCelsius function definition? 
6.PROC FCMPの次のステップで、ToCelsius関数の定義を開始するために必要な文はどれですか。

proc fcmp outlib=lib3.func.weather;
    __________ ToCelsius(Fah);
        Cel=round((Fah-32)*5/9,.01);
        return(Cel);
    endsub;
run;

1 / 1点


STARTFUNC

FUNCDEF

FUNCTION
correct

DEF


正解
The FUNCTION statement begins the function definition. It names the function and defines the arguments and a return value.
FUNCTION ステートメントは、関数定義を開始します。これは、関数に名前を付け、引数と戻り値を定義します。


7.Complete the FUNCTION statement to create a function named sumfunc that consists of two numeric arguments. 
7.FUNCTIONステートメントを完成させて、2つの数値引数からなるsumfuncという名前の関数を作成します。

function sumfunc _______________;
    sum = addend1+addend2;
    return(sum);
endsub;

1 / 1点


(addend1 addend2)

(addend1 $ addend2 $)

(addend1 $, addend2 $)

(addend1, addend2)
correct

正解
Arguments must be separated with a comma. Numeric arguments are not followed by a dollar sign.
引数はカンマで区切らなければならない。数値引数の後にはドル記号を付けないでください。



8.In general, functions change the value of existing columns while CALL ROUTINES return a value.
8.一般的に、関数は既存の列の値を変更し、CALL ROUTINESは値を返します。

0 / 1点

False

True
correct

不正解
CALL ROUTINES typically change the value of existing columns while functions return a value. Review 
CALL ROUTINESは通常、既存の列の値を変更し、関数は値を返します。レビュー 


9.What is the function of the CMPLIB= option?
9.CMPLIB=オプションの機能は何ですか？

1 / 1点

The CMPLIB= option specifies the three-level name of the package where the compiled function will be stored.
CMPLIB= オプションは、コンパイルされた関数が格納されるパッケージの 3 レベル名を指定します。

The CMPLIB= option specifies the table (or tables) that SAS searches for a package that contains the desired function.
CMPLIB= オプションでは、SAS が目的の関数を含むパッケージを検索するテーブル（複数可）を指定します。
Correct

The CMPLIB= option specifies the two-level name of the package where the compiled function will be stored.
CMPLIB= オプションは、コンパイルした関数を格納するパッケージの 2 階層名を指定します。

The CMPLIB= option specifies the column (or columns) that SAS searches for a package that contains the desired function.
CMPLIB= オプションは、SAS が目的の関数を含むパッケージを検索する列を指定します。


正解
The CMPLIB= option specifies the table (or tables) that SAS searches for a package that contains the desired function.
CMPLIB=オプションは、SASが目的の関数を含むパッケージを検索するテーブル（複数可）を指定します。



10.Which statement is true concerning the following PROC FCMP statement? 
10.PROC FCMPの次の記述について、正しいものはどれか。

proc fcmp outlib=pg3.funcs.misc; 

0 / 1点

The custom function will be stored in the misc library.
uncorrect

The custom function will be stored in the pg3.funcs table.

The custom function will be stored in the pg3.funcs package.
uncorrect

The custom function will be stored in the SAS table pg3.
uncorrect

不正解
Custom functions are stored in a package within a SAS table. 
カスタム関数は、SAS テーブル内のパッケージに格納されます。
The OUTLIB= option consists of a three-level name. The first two levels refer to the SAS table (  ), 
and the third level refers to the package. Review .
OUTLIB=オプションは、3レベルの名前から構成されます。最初の2つのレベルは、SASテーブル（ ）を参照します。
を参照し、第 3 レベルはパッケージを参照します。レビュー .


*****************************************************************************************************************************
